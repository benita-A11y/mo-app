# 「墨」日程/今日页重新设计提案 v3

> 基于用户提供的 9 张「P 计划本」参考图，结合「墨」现有 5-Tab 结构（今日 / 日程 / 目标 / 待办 / 复盘）与莫兰迪/薰衣草设计系统，给出可落地的页面导航、层级分类、侧栏与响应式方案。

---

## 一、参考图核心结构拆解

### 1.1 全局导航：左侧抽屉 + 底部悬浮 Dock

| 区域 | 参考图表现 | 作用 |
|------|-----------|------|
| **左侧抽屉** | 头像/设置、兑换 Banner、智能清单（收集箱/逾期/今天/本周/本月）、标签（工作/学习/生活） | 全局入口 + 层级切换；本质上是「按时间维度 + 标签维度」的过滤器 |
| **底部悬浮 Dock** | 4 个主入口：今日 ✓、指南针/目标、列表、+ 号 FAB | 一级导航，采用胶囊悬浮样式，不贴底，视觉更轻盈 |
| **顶部工具栏** | ☰ 菜单按钮、标题「8月，今天/W35,本周/2026/8」+ 绿色箭头、礼物、搜索、# 标签、⋯更多 | 标题即当前视图，箭头用于展开日期/周/月选择器 |
| **右侧任务侧栏** | 收集箱面板，可拖到中间主区域 | 存放未安排任务，作为拖拽源 |

### 1.2 视图层级：天 / 周 / 月 三视图共用同一入口

参考图里所有视图都在同一个「今日/日程」入口下切换，通过顶部标题右侧的绿色箭头展开选择器：

- **天视图**：标题「8月，今天 ▾」→ 展开整月迷你日历；下方是「每日重点/每日总结」卡片 + 当天任务按标签分组列表。
- **周视图**：标题「W35, 本周 ▾」→ 展开整月日历，但高亮当前这一周（横向一整行绿色）；下方是 2×4 的 7 天卡片布局 + 左侧本周重点/总结。
- **月视图**：标题「2026/8 ▾」→ 展开「年 × 月」双列选择器；下方是整月任务密度月历，点击某天显示当天任务。

### 1.3 任务展示逻辑：按「标签」分组，而不是按「时间块」

参考图里任务不是画在时间轴上，而是：
- 按 `#工作`、`#学习`、`#生活` 等标签垂直分组；
- 每条任务左侧有一个圆环复选框；
- 右侧显示收集箱/任务抽屉，未安排任务可拖到左侧对应标签区域。

### 1.4 收集箱位置：右侧常驻抽屉，移动端可收起

- 电脑/平板：右侧固定宽度的收集箱面板，直接列出未安排任务，可拖拽。
- 手机：收集箱以抽屉/浮层面板呈现，通过顶部或侧边的按钮展开，避免主内容区被挤占。

---

## 二、与「墨」现状的映射

| 参考图概念 | 「墨」现状 | 建议映射 |
|-----------|----------|---------|
| 左侧抽屉里的「收集箱」 | 「待办」Tab 顶部有「灵感/收集箱」 | 可保留在日程页右侧抽屉，与待办页收集箱共用同一数据源 `store.inbox` |
| 左侧抽屉里的「今天/本周/本月」 | 日程 Tab 内部已有 day/week/month 三视图 | 把这些入口同时放在日程页左侧抽屉，作为快速跳转 |
| 左侧抽屉里的「逾期」 | 日程过滤器里已有 overdue 筛选 | 作为日程页左侧抽屉的固定项 |
| 标签（工作/学习/生活） | 墨已有 6 色 tag 分类，日程侧栏已有「标签」Tab | 直接复用，但改为左侧抽屉常驻展示 |
| 底部 4 入口 | 墨目前有 5 个底部 Tab | **建议保持 5 个功能入口不变**，但把底部栏改成参考图风格的「悬浮胶囊 Dock」，美观且不遮挡内容 |
| 标题旁的 ▾ 箭头 | 已部分实现（天视图展开迷你月历） | 统一到天/周/月三视图：天展开月历、周展开「周选择器」、月展开「年月选择器」 |

---

## 三、推荐方案：三层布局 + 响应式适配

### 3.1 页面整体骨架（以「日程」Tab 为例）

```
┌─────────────────────────────────────────────────────────────┐
│  Header：☰  标题 + ▾   [礼物] [搜索] [#] [⋯]                  │
├──────────┬──────────────────────────────┬───────────────────┤
│          │                              │                   │
│  左侧    │                              │    右侧           │
│  导航    │       主内容区               │   任务/收集箱     │
│  抽屉    │     天 / 周 / 月 视图        │    抽屉           │
│  (可收起)│                              │   (可收起)        │
│          │                              │                   │
└──────────┴──────────────────────────────┴───────────────────┘
              └────── 底部悬浮 Dock（5 入口）──────┘
```

### 3.2 左侧导航抽屉内容

建议固定为 5 大板块：

1. **智能清单**
   - 收集箱（未安排任务数）
   - 逾期
   - 今天
   - 本周
   - 本月
2. **标签列表**
   - 工作 / 学习 / 生活（以及墨自定义标签）
   - 每个标签右侧显示未完成数量
3. **四象限快捷**
   - 重要且紧急 / 重要不紧急 / 紧急不重要 / 不紧急不重要
4. **视图切换**
   - 天 / 周 / 月 三个小按钮（作为顶部 Tab 的补充）
5. **操作**
   - 新建分类 / 新建标签

> 移动端：左侧抽屉通过 ☰ 按钮唤出，覆盖层 80% 宽度。
> 桌面端：左侧抽屉默认折叠为窄边栏（图标+文字），hover 或固定展开。

### 3.3 右侧任务/收集箱抽屉

- **默认显示「收集箱」**：未安排任务（`inbox` + `backlog` 中无日期的）按录入时间倒序。
- **可切换为「全天任务」**：当天有日期但无具体时间、未完成的任务。
- **可切换为「标签任务」**：点击左侧标签后，右侧显示该标签下全部未完成任务。
- **支持拖拽**：拖到中间主区域的天/周/月/四象限，即完成安排。

> 桌面端：右侧抽屉默认常驻，宽度 280–320px。
> 平板：默认收起，通过顶部按钮展开。
> 手机：作为底部浮层/抽屉，通过右下角或顶部按钮展开。

### 3.4 主内容区：按视图分别设计

#### A. 天视图

```
标题「8月24日，今天 ▾」+ 展开后迷你月历
每日重点 / 每日总结 卡片
标签分组任务列表（#工作 / #学习 / #生活…）
每个分组内任务可拖拽排序、拖到其他分组换标签
```

#### B. 周视图

```
标题「W35，本周 ▾」+ 展开后整月周选择器（高亮当前周）
本周重点 / 本周总结 卡片
7 天 2×4 网格 或 横向 7 列
每天显示：日期标签 + 任务数 + 任务列表（按标签小点区分）
```

#### C. 月视图

```
标题「2026/8 ▾」+ 展开后「年 × 月」双列选择器
本月重点 / 本月总结 卡片
标准 7×6 月历网格
有任务日期显示彩色小条/圆点
点击日期 → 右侧抽屉显示该天任务 或 弹出日任务 Sheet
```

---

## 四、底部导航改造：5 入口悬浮胶囊 Dock

参考图底部是 4 入口悬浮胶囊 + 右侧 + 号。墨现有 5 个功能，建议：

- **保持 5 个入口**：今日 / 日程 / 目标 / 待办 / 复盘
- **样式改为悬浮胶囊 Dock**：
  - 背景：毛玻璃 `rgba(255,255,255,.75)` + `backdrop-filter`
  - 圆角大胶囊（约 28–32px）
  - 底部留安全距离 `calc(12px + env(safe-area-inset-bottom))`
  - 当前项用主题色填充背景/白字
  - 图标 + 文字垂直排列，但文字更小更轻
- **新建按钮**：保留现有右下角 + 号 FAB，或改为 Dock 右侧独立的 + 按钮（参考图风格）。

> 这样底部导航不贴边，视觉上更透气；参考图看起来整洁很大程度来源于此。

---

## 五、响应式断点策略

| 断点 | 布局 | 侧栏状态 |
|-----|------|---------|
| **< 640px 手机** | 单列；左侧抽屉全屏覆盖；右侧抽屉底部浮层 | 默认收起 |
| **640–1024px 平板/大手机横屏** | 双栏：左侧抽屉折叠为窄边栏，主内容 + 右侧抽屉收起 | 右侧抽屉按需展开 |
| **≥ 1024px 桌面/大屏** | 三栏：左侧抽屉展开、主内容、右侧抽屉常驻 | 默认展开 |

CSS 断点建议：

```css
/* 手机优先 */
.sched-body{flex-direction:column}
.sched-left,.sched-right{display:none} /* 抽屉模式 */

/* 平板 */
@media (min-width: 640px) {
  .sched-left{display:flex;width:220px}
  .sched-right{position:absolute;right:0;top:0;z-index:10}
}

/* 桌面 */
@media (min-width: 1024px) {
  .sched-left{width:260px}
  .sched-right{position:static;display:flex;width:300px}
}
```

---

## 六、关键交互细节（对齐参考图）

1. **标题箭头 ▾**：
   - 天视图：展开整月迷你日历（已部分实现）。
   - 周视图：展开整月日历，但把当前周那一行高亮，点击其他周行切换。
   - 月视图：展开「年份列 + 月份列」双列选择器。

2. **拖拽方向**：
   - **右侧收集箱 → 中间主区域**：安排任务日期/标签/象限。
   - **中间任务 → 左侧标签/时间维度**：重新分类或改期。
   - 桌面端同时支持鼠标拖拽；移动端降级为长按菜单。

3. **任务分组展示**：
   - 天视图按标签分组（参考图核心）。
   - 周/月视图按日期展示，但任务卡片左侧显示标签色点。

4. **四象限与标签的关系**：
   - 四象限用于优先级筛选，标签用于分类展示。
   - 左侧抽屉中「四象限」作为筛选器，标签作为分组器。

---

## 七、实施建议清单（按优先级）

### 阶段 1：框架与响应式（先打地基）
1. 把日程页改写成三栏骨架：左侧抽屉 + 主内容 + 右侧抽屉。
2. 添加响应式断点，手机/平板/桌面分别处理侧栏。
3. 把底部固定 Tabbar 改成悬浮胶囊 Dock（先改样式，保持 5 入口）。

### 阶段 2：左侧导航抽屉
4. 提取「收集箱/逾期/今天/本周/本月」为抽屉菜单。
5. 提取标签列表 + 四象限快捷为抽屉菜单。
6. 点击菜单项时：切换日程视图 or 设置过滤条件。

### 阶段 3：主内容区重排
7. 天视图改为「按标签分组列表」而非四象限网格/时间轴。
8. 周视图改为 7 天卡片网格（2×4 或横向 7 列）。
9. 月视图保持月历，但任务以彩色小条展示。

### 阶段 4：右侧收集箱抽屉与拖拽
10. 把当前日程侧栏的「收集箱/标签/全天」改为右侧抽屉，默认显示收集箱。
11. 实现右侧 → 中间、中间 → 左侧/右侧的双向拖拽。
12. 移动端改为长按菜单或底部拖出面板。

### 阶段 5：日期选择器统一
13. 天视图箭头展开月历。
14. 周视图箭头展开「周选择器」。
15. 月视图箭头展开「年月选择器」。

---

## 八、需要用户确认的三个关键决策

1. **底部导航数量**：保持 5 个（今日/日程/目标/待办/复盘）并改成悬浮 Dock，还是合并为 4 个（把「待办」合并到日程右侧抽屉，或把「复盘」收进更多）？
2. **日程页入口是否同时出现在「今日」Tab**：参考图所有视图都在一个入口；墨当前「今日」和「日程」是分开的。是否把「今日」作为日程 Tab 的一个默认子视图，还是保持独立？
3. **拖拽优先级**：先做桌面端三栏拖拽，还是移动端长按菜单也一起做？

---

## 九、参考设计系统延续

- 颜色：继续用莫兰迪薰衣草/蜜桃/抹茶/桂花/樱，保持 `--primary` 主题色。
- 圆角：大卡片 16–18px，小按钮 12–14px，底部 Dock 28–32px。
- 阴影：保持柔和 `0 6px 24px rgba(0,0,0,.06)`，Dock 用更强的上浮感。
- 字体：沿用 Noto Sans SC / 苹方，标题 17–18px，正文 13–14px。

---

*这份提案先写到这里。宝宝确认方案（或告诉我先做哪一阶段）后，我再开始改代码。*
