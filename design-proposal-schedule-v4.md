# 「墨」日程页人格化设计提案 v4

> 以 **天秤座（和谐·美感·平衡·不纠结）** + **ISFJ（稳定·有序·被需要·低认知负荷）** 为人物画像，重新设计「日程」Tab 的使用流程与页面导航，确保流程闭环、视觉平静、交互可预期。
>
> 设计范围：仅改动「日程」Tab 内部；今日 / 目标 / 待办 / 复盘 四个 Tab 保持原样。

---

## 一、人物画像：谁在使用这个平台？

### 1.1 天秤座画像

| 核心需求 | 在产品里的表现 |
|---------|--------------|
| **视觉和谐** | 界面不能乱，颜色要柔和、统一、有呼吸感 |
| **选择不纠结** | 下一步该做什么，产品要替用户想一步，给出默认推荐 |
| **关系与平衡** | 工作/学习/生活要「雨露均沾」，不能失衡 |
| **美感驱动** | 完成一页漂亮的日程本身就是动力 |

**典型场景**  
> 早上打开「墨」，看到今天已经按标签分好类、颜色和谐、重点突出，心情就好了一半。如果系统能告诉她「今天工作已经够多了，留 30 分钟给生活」，她会觉得被照顾到。

### 1.2 ISFJ 画像

| 核心需求 | 在产品里的表现 |
|---------|--------------|
| **稳定有序** | 每天流程一致，入口位置不变，减少重新学习成本 |
| **被需要感** | 完成打勾、看到「今日已完成 X 件」会有成就感 |
| **低认知负荷** | 一次只呈现必要信息，不堆功能、不炫技 |
| **细心负责** | 喜欢提前安排，在意逾期提醒和复盘 |

**典型场景**  
> 她会提前一天晚上把明天任务放进日程；如果早上发现有逾期任务，会焦虑。产品要温和地提示「昨天漏了 2 件，先安排到今天上午？」，而不是红色警告。

### 1.3 两种人格的交集设计原则

| 原则 | 含义 | 落地方式 |
|-----|------|---------|
| **视觉平静（Visual Calm）** | 不刺激、不拥挤 | 莫兰迪色、大量留白、圆角、无锐利边框 |
| **流程显而易见（Obvious Flow）** | 用户永远知道「我在哪 / 要做什么 / 下一步去哪」 | 固定结构、面包屑式标题、状态反馈 |
| **减少决策疲劳** | 默认即最优，少让用户做艰难选择 | 智能清单自动排序、拖拽自动分类、AI 推荐时段 |
| **正向反馈** | 每个动作都有温柔的确认 | 完成动画、柔和 toast、进度环 |
| **容错与回退** | 误操作能轻松撤销 | 删除二次确认、拖拽失败退回原位 |
| **对称与平衡** | 工作/生活/学习一眼可见 | 标签分组、每日总结提示是否失衡 |

---

## 二、使用流程闭环：从「灵感」到「更好的自己」

整个平台的五个 Tab 不是孤立页面，而是一个 **「收集 → 安排 → 执行 → 复盘 → 优化」** 的闭环。

```
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│  收集   │ → │  安排   │ → │  执行   │ → │  复盘   │ → │  优化   │
│ (待办)  │    │ (日程)  │    │ (今日)  │    │ (复盘)  │    │ (目标)  │
└─────────┘    └─────────┘    └─────────┘    └─────────┘    └─────────┘
     ↑___________________________________________________________↓
                              形成闭环
```

### 2.1 五步闭环详解

| 阶段 | 主 Tab | 用户心理 | 产品要做什么 | 关键动作 |
|-----|--------|---------|------------|---------|
| **1. 收集** | 待办 | 「脑子里有一堆事，先丢进去」 | 提供最低成本的入口，快速 dump | 右下角 + 号 / 语音 / 快速输入 → 进入 `store.inbox` |
| **2. 安排** | 日程 | 「这些任务该放到哪天、哪个标签？」 | 让安排变得像整理桌面一样直观 | 右侧收集箱 → 拖到左侧对应日期/标签 |
| **3. 执行** | 今日 | 「今天我只关心今天要做什么」 | 只显示今天，按重要性/时段排序 | 今日 Tab 展示今日任务时间线 + 今日专注 |
| **4. 复盘** | 复盘 | 「这周/月过得怎么样？」 | 温和呈现数据，引导用户写一句总结 | 月历视图、完成率、情绪/总结输入 |
| **5. 优化** | 目标 | 「下个月我要朝什么方向走？」 | 把复盘结果转化为下周/月目标 | 周目标、月目标、标签平衡建议 |

### 2.2 为什么这样闭环？

- **天秤座视角**：五个 Tab 像五个「房间」，每个房间功能单一、风格统一，不会有「这个按钮到底在哪」的混乱。
- **ISFJ 视角**：每天使用顺序固定——早上「今日」执行，晚上「日程」安排明天，周末「复盘」+「目标」，形成可预期的仪式。

### 2.3 日程 Tab 在闭环中的核心位置

日程不是「又一个任务列表」，而是 **「收集」到「执行」之间的调度中枢**。

```
待办（收集箱） → 日程（安排） → 今日（执行）
                     ↑                ↓
                 复盘/目标 ← 完成记录
```

---

## 三、导航架构：少即是多，但一应俱全

### 3.1 全局导航：5 个一级入口（保持不变）

底部悬浮胶囊 Dock：

| 入口 | 作用 | 何时去 |
|-----|------|--------|
| **今日** | 执行今天的任务 | 早上/随时 |
| **日程** | 安排任务到具体日期和标签 | 前一天晚上/有空时 |
| **目标** | 查看长期目标、设定周/月目标 | 周末/月初 |
| **待办** | 快速收集灵感/任务 | 随时 |
| **复盘** | 查看完成率、写总结 | 每晚/周末 |

> 设计原则：入口数量不增减，只把样式改成更柔和的悬浮胶囊，降低视觉重量。

### 3.2 日程 Tab 内部导航：三层抽屉 + 主视图

参考图的核心结构是 **「左导航 + 中间视图 + 右收集箱」**，我们直接沿用。

```
┌──────────────────────────────────────────────────────────────┐
│  ☰   标题 + ▾    [搜索] [#标签] [⋯]                          │
├──────────┬─────────────────────────────────┬─────────────────┤
│  左侧    │                                 │    右侧         │
│  导航    │      主视图                      │   收集箱        │
│  抽屉    │   天 / 周 / 月 三视图           │   抽屉          │
│          │                                 │                 │
└──────────┴─────────────────────────────────┴─────────────────┘
              └────── 底部悬浮 Dock ──────┘
```

#### 左侧抽屉内容（智能清单 + 标签 + 四象限）

| 区域 | 项目 | 作用 |
|-----|------|------|
| **智能清单** | 收集箱、逾期、今天、本周、本月 | 按时间维度快速切换主视图，并带数量徽标 |
| **标签** | 工作 / 学习 / 生活 / 健康 / 财务 / 其他 | 按标签维度筛选当前视图 |
| **四象限** | 重要紧急 / 重要不紧急 / 紧急不重要 / 不紧急不重要 | 快速关注优先级 |

> 设计原则：左侧抽屉常驻于桌面，手机端通过 ☰ 唤出。所有入口一眼可见，不需要记忆。

#### 右侧抽屉内容（收集箱 + 全天任务 + 标签任务）

| Tab | 内容 | 用途 |
|-----|------|------|
| **收集箱** | 从「待办」页丢进来的未安排任务 | 拖到中间安排 |
| **全天任务** | 已设日期但无具体时段的任务 | 拖到某天/某个时间槽 |
| **标签任务** | 按选中标签聚合的任务 | 方便批量查看与拖动 |

> 核心交互：**右侧收集箱 → 拖到左侧/中间主区域**，完成从「收集」到「安排」的闭环。

---

## 四、天视图布局：参考图还原 + 优化

### 4.1 参考图结构拆解

参考图 `ba65b91e5174f0060d5f7a775f694cad.png` 的天视图包含：

1. **顶部工具栏**：☰ 菜单 | 「8月，今天」+ 绿色 ▾ 箭头 | 礼物 | 搜索 | # | ⋯
2. **日期条**：周一~周日横向排列，今天（24号）用绿色圆点高亮
3. **每日重点/总结卡片**：可切换的输入区，左侧绿色「每日重点」Tab
4. **主内容区**：按标签分组（#工作、#学习），每条任务左侧圆环复选框
5. **右侧收集箱抽屉**：📥 收集箱，列出未安排任务，圆环复选框
6. **底部悬浮 Dock**：胶囊形状 + 右侧独立 + 号 FAB

### 4.2 天视图布局设计

```
┌──────────────────────────────────────────────────────────┐
│  ☰   8月，今天  ▾      [🔍] [#] [⋯]                      │
├──────────────────────────────────────────────────────────┤
│  周一  周二  周三  周四  周五  周六  周日                │
│  [24]  25   26   27   28   29   30                       │
├──────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────┐  │
│  │ [每日重点] [每日总结]                ❄️  ✕         │  │
│  │ 今天最想完成什么？                                     │  │
│  └────────────────────────────────────────────────────┘  │
├─────────────────────────────┬────────────────────────────┤
│                             │                            │
│  #工作                       │   📥 收集箱                │
│  ○ 更新项目文档               │   ○ 规划下季度...          │
│                             │   ○ 复习课堂笔记           │
│  #学习                       │   ○ 规划周末旅行           │
│  ○ 阅读第5章                 │   ○ 我爱你                 │
│                             │                            │
│  #生活                       │   （可拖拽到左侧）         │
│  ...                         │                            │
│                             │                            │
└─────────────────────────────┴────────────────────────────┘
              └────── 悬浮 Dock ──────┘        [+]
```

### 4.3 各区域设计细节

#### A. 顶部工具栏

| 元素 | 设计 |
|-----|------|
| 左侧 ☰ | 展开左侧导航抽屉 |
| 标题「8月，今天」 | 当前视图名称；点击或点 ▾ 展开选择器 |
| ▾ 绿色箭头 | 天视图展开「整月迷你日历」；选中日期后自动收起 |
| 搜索 🔍 | 全局搜索任务（未来版本可先 toast「搜索即将上线」） |
| # 标签 | 快速按标签筛选当前视图 |
| ⋯ 更多 | 视图设置、导出、打印等 |

> 天秤座/ISFJ 友好：标题明确、按钮对称、绿色箭头暗示「可展开但不强迫」。

#### B. 日期条

- 横向 7 天，今天用 **主题色渐变圆点** 突出（不是方框，更柔和）
- 左右滑动可翻周
- 点击某天切换到该日
- 未选中的日期用浅灰色文字，降低干扰

#### C. 每日重点 / 每日总结卡片

```
┌────────────────────────────────────────┐
│ [每日重点] [每日总结]          ❄️  ✕    │
│ ──────────────────────────────────────  │
│ 今天最想完成什么？                      │
└────────────────────────────────────────┘
```

- 默认展开「每日重点」，用户可写 1~3 句话
- 点击「每日总结」切换，晚上复盘时填写
- 右侧小图标：AI 建议重点 / 关闭卡片
- 卡片常驻于天视图顶部，帮助用户聚焦

#### D. 主内容区：按标签分组列表

这是参考图的核心，也是天秤座最爱的「分类清晰、视觉平衡」。

```
#工作                         ← 标签色点 + 名称 + 任务数
  ○ 更新项目文档               ← 左侧圆环复选框

#学习
  ○ 阅读第5章

#生活
  ...
```

- 每个标签组用 **左侧色块竖条** 或 **# 色点** 标识
- 任务左侧圆环复选框，完成时圆环填充主题色并轻微缩放动画
- 空标签组显示「暂无任务，从右侧拖入一个吧」占位
- 长按任务弹出菜单（移动端）

### 4.4 圆环复选框设计

为了 ISFJ 的「完成仪式感」和天秤座的「柔和美感」：

- 未完成：细圆环，灰色
- 完成时：圆环填充主题色，中心出现 ✓，伴随 200ms 弹性缩放
- hover/长按：圆环颜色加深，提示可点击

---

## 五、右侧收集箱 → 左侧主区域的拖拽交互

### 5.1 交互目标

让用户像 **「把右侧抽屉里的任务卡片，放到中间对应标签组」** 一样自然。

### 5.2 桌面端拖拽流程

```
1. 用户按住右侧收集箱中的任务卡片
2. 卡片半透明缩小跟随鼠标
3. 拖到中间某标签组上方时，该组背景轻微高亮
4. 松开 → 卡片落入该标签组最上方
5. 系统自动设置：dueDate = 当前天视图日期，category = 目标标签组
6. 右侧收集箱中该任务消失，中间对应组出现
7. 显示 toast：「已安排到 8/24 · 工作」
```

### 5.3 拖拽到不同位置的语义

| 拖放目标 | 结果 |
|---------|------|
| 中间某标签组 | 任务 dueDate = 当前日期，category = 该标签 |
| 周视图某天 | dueDate = 该日期，保留原 category |
| 月视图某天 | dueDate = 该日期，保留原 category |
| 四象限区域 | 同时设置优先级/重要紧急属性 |
| 全天任务抽屉 | 清除 startTime，仅保留 dueDate |

### 5.4 视觉反馈

| 阶段 | 反馈 |
|-----|------|
| dragstart | 卡片 `opacity:0.8` + `scale(1.05)` + 轻微阴影 |
| dragover 合法区域 | 目标区域 `background:rgba(var(--primary-rgb),0.08)` + `border-color:var(--primary)` |
| drop | 卡片落入位置有 200ms 高亮闪烁 |
| 完成 | 右侧抽屉该卡片滑出消失，左侧目标组淡入 |
| 非法放置 | 卡片弹回原位，toast「拖到标签组或日期上即可安排」 |

### 5.5 移动端降级方案

手机无法方便拖拽，用 **长按菜单** 实现相同流程：

```
长按收集箱任务 → 弹出菜单：
  ├─ 安排到今天
  ├─ 安排到明天
  ├─ 选择日期…
  ├─ 分到标签 ▸
  │   ├─ 工作
  │   ├─ 学习
  │   ├─ 生活
  │   └─ …
  ├─ 标为已完成
  ├─ 编辑
  └─ 删除
```

> ISFJ 友好：长按时间 480ms，避免误触；菜单层级不超过 2 层。

### 5.6 容错与回退

- 拖拽到空白处 → 卡片弹回，无变化
- 误删任务 → 5 秒内 toast 显示「撤销」按钮
- 从收集箱拖出后未成功放置 → 任务仍保留在收集箱

---

## 六、人格化细节清单

### 6.1 天秤座专属细节

| 位置 | 设计 |
|-----|------|
| 颜色 | 所有标签色用莫兰迪低饱和色，避免刺眼的正红正绿 |
| 圆角 | 卡片 16px、按钮 12px、复选框 50%（圆环） |
| 对称 | 左右抽屉宽度一致（桌面端），Dock 居中悬浮 |
| 空状态 | 「今天很清闲，去安排一件让自己开心的事吧」 |
| 标签平衡 | 当某天某类任务过多时， gently 提示「今天工作 5 件，生活 0 件，要不要匀一点？」 |

### 6.2 ISFJ 专属细节

| 位置 | 设计 |
|-----|------|
| 入口固定 | 底部 Dock 5 个入口顺序永不改变 |
| 默认视图 | 打开日程 Tab 默认显示「今天」，减少跳转 |
| 完成反馈 | 打勾后显示「今日已完成 X/5 · 继续加油」 |
| 逾期处理 | 逾期任务不标红，用柔和的紫色提示「有 2 件想回到你视线里」 |
| 每日总结 | 晚上 21:00 温和提醒「今天过得怎么样？写一句总结吧」 |

---

## 七、响应式适配策略

| 屏幕宽度 | 布局 | 左右抽屉 | 拖拽 |
|---------|------|---------|------|
| **< 760px 手机** | 单栏 | 左右抽屉均收起，通过 ☰ / 📥 按钮唤出为覆盖层 | 长按菜单替代拖拽 |
| **760px ~ 1024px 平板** | 双栏 | 左侧抽屉常驻，右侧收集箱可收起为悬浮按钮 | 支持拖拽 |
| **≥ 1024px 桌面** | 三栏 | 左右抽屉均常驻 | 完整拖拽 |

> 特别说明：桌面端日程 Tab 自动放宽容器（`.wide-mode`），三栏不局促；其他 Tab 保持原有 480~520px 窄屏体验。

---

## 八、文案语气指南

整体语气：**温柔、肯定、不催促、像一位细心的朋友**。

| 场景 | 推荐文案 | 避免文案 |
|-----|---------|---------|
| 空收集箱 | 「收集箱很干净，去捕捉一个新想法吧 ✨」 | 「暂无数据」 |
| 逾期 | 「有 2 件任务想回到你视线里」 | 「您有 2 个任务已逾期！」 |
| 完成 | 「今日已完成 3/5，继续加油 💜」 | 「任务完成」 |
| 拖拽成功 | 「已安排到 8/24 · 工作」 | 「操作成功」 |
| 删除 | 「确定要移除「更新项目文档」吗？可以撤销哦」 | 「确认删除？」 |
| 标签失衡 | 「今天工作很多，留一点时间给生活吧」 | 「生活任务为 0」 |

---

## 九、下一步实现清单

1. **重构天视图渲染**：改为按标签分组列表，每条任务使用圆环复选框
2. **完成右侧收集箱抽屉**：默认显示收集箱，支持切换到「全天/标签」
3. **实现拖拽交互**：右侧 → 中间标签组，含视觉反馈与错误回退
4. **实现长按菜单**：移动端拖拽的降级方案
5. **完成圆环复选框组件**：未完成/完成两种状态 + 动画
6. **适配响应式**：三栏/双栏/单栏断点
7. **文案替换**：按人格化文案指南统一所有提示
8. **版本号升级**：本地预览 → 部署

---

_本提案基于 `design-proposal-schedule-v3.md` 扩展，聚焦人格化设计与参考图天视图的落地细节。_
